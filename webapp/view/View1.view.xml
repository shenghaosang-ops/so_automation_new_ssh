<!--
/******************************************************************
 * SO Automation Application - Main View
 * 
 * This view implements a three-section interface for SO processing:
 * 1. File Upload: Handles Excel file selection and validation
 * 2. Process Configuration: Manages customer selection, rules, and batch size
 * 3. Results Display: Shows processing results in a sortable/filterable table
 * 
 * Author: Junli Ye
 * Version: 1.0.0
 * Last Updated: August 12, 2025
 ******************************************************************/
-->
<mvc:View
    controllerName="yegeoaiso.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:u="sap.ui.unified"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core">

    <Page
        id="page"
        title="销售订单自动化演示"
        showNavButton="true"
        navButtonPress="onNavBack"
        backgroundDesign="Transparent">

        <content>
            <!--
            /************************
             * File Upload Section
             * 
             * Components:
             * - FileUploader: Accepts only .xlsx files
             * - MessageStrip: Shows selected file name and status
             ************************/
            -->
            <Panel
                expandable="true"
                expanded="true"
                headerText="上传CSV/Excel文件"
                width="auto"
                class="sapUiNoContentPadding">
                <headerToolbar>
                    <Toolbar style="Clear">
                        <Title text="上传CSV/Excel文件"/>
                        <ToolbarSpacer/>
                        <core:Icon src="sap-icon://upload" class="sapUiTinyMarginEnd"/>
                    </Toolbar>
                </headerToolbar>
                <content>
                    <VBox class="sapUiSmallMargin">
                        <HBox alignItems="Center" class="sapUiSmallMargin">
                            <u:FileUploader
                                id="fileUploader"
                                buttonText="选择CSV/Excel文件"
                                fileType="csv,xlsx"
                                change="onFileSelect"
                                style="Emphasized"
                                useMultipart="false"
                                sendXHR="true"
                                tooltip="上传CSV或Excel文件"/>
                            <Button
                                id="extractButton"
                                text="提取数据"
                                press="onExtractPress"
                                type="Emphasized"
                                icon="sap-icon://table-view"
                                class="sapUiTinyMarginBegin"
                                tooltip="从CSV或Excel文件提取数据"/>
                            <MessageStrip
                                id="fileNameStrip"
                                text="未选择文件。支持格式：CSV (.csv), Excel (.xlsx)"
                                type="Information"
                                showIcon="true"
                                class="sapUiTinyMarginBegin"/>
                        </HBox>
                        <!-- Preview Panel with Expand/Collapse -->
                        <Panel
                            id="previewPanel"
                            expandable="true"
                            expanded="false"
                            headerText="Excel数据预览"
                            class="sapUiTinyMarginTop previewPanel">
                            <headerToolbar>
                                <Toolbar>
                                    <Title text="Excel数据预览（前5列）"/>
                                    <ToolbarSpacer/>
                                    <Text text="{viewData>/rowCount} 行已加载" class="sapUiTinyMarginEnd"/>
                                </Toolbar>
                            </headerToolbar>
                            <content>
                                <Table
                                    id="previewTable"
                                    items="{
                                        path: 'viewData>/excelData',
                                        length: 5
                                    }"
                                    growing="false"
                                    class="sapUiTinyMarginTop">
                                    <columns>
                                        <!-- Columns will be created dynamically -->
                                    </columns>
                                    <items>
                                        <ColumnListItem>
                                            <cells>
                                                <!-- Cells will be created dynamically -->
                                            </cells>
                                        </ColumnListItem>
                                    </items>
                                </Table>
                            </content>
                        </Panel>
                    </VBox>
                </content>
            </Panel>

            <!--
            /*************************************
             * Process Configuration Section
             * 
             * Left Side:
             * - Customer Selection Dropdown
             * - Batch Size Selection (1/5/10)
             * - Start Processing Button
             * 
             * Right Side:
             * - Rule Configuration Preview/Editor
             * - Edit/Cancel Buttons
             *************************************/
            -->
            <Panel
                expandable="true"
                expanded="true"
                headerText="规则配置"
                width="auto"
                class="sapUiNoContentPadding">
                <headerToolbar>
                    <Toolbar style="Clear">
                        <Title text="规则配置"/>
                        <ToolbarSpacer/>
                        <core:Icon src="sap-icon://settings" class="sapUiTinyMarginEnd"/>
                    </Toolbar>
                </headerToolbar>
                <content>
                    <HBox class="sapUiSmallMargin" renderType="Bare">
                        <!-- Left Section: Customer and Batch Size -->
                        <VBox width="300px" class="sapUiTinyMarginEnd">
                            <!-- Customer Selection -->
                            <Label text="客户" required="true" class="sapUiTinyMarginBottom"/>
                            <!-- <Select
                                id="customerSelect"
                                change="onCustomerChange"
                                class="sapUiSmallMarginBottom"
                                width="100%">
                                <core:Item key="customerA" text="客户A"/>
                                <core:Item key="customerB" text="客户B"/>
                            </Select> -->
                            <Select
                                id="customerSelect"
                                change="onCustomerChange"
                                class="sapUiSmallMarginBottom"
                                width="100%"
                                items="{customerRules>/customers}">
                                <core:Item key="{customerRules>key}" text="{customerRules>name}"/>
                            </Select>
                            <!-- Batch Size Selection -->
                            <Label text="批次大小" required="true" class="sapUiTinyMarginBottom"/>
                            <Select
                                id="batchSizeSelect"
                                class="sapUiSmallMarginBottom"
                                width="100%">
                                <core:Item key="1" text="每批次1条记录"/>
                                <core:Item key="5" text="每批次5条记录"/>
                                <core:Item key="10" text="每批次10条记录"/>
                            </Select>
                            <!-- Action Button -->
                            <Button
                                id="btnAutomation"
                                text="启动AI Mapping"
                                type="Emphasized"
                                width="100%"
                                icon="sap-icon://activate"
                                press="onStartAutomation"/>
                        </VBox>
                        <!-- Right Section: Rule Preview -->
                        <VBox width="100%" class="sapUiTinyMarginBegin">
                            <HBox justifyContent="SpaceBetween" class="sapUiTinyMarginBottom">
                                <Label text="规则配置" required="true"/>
                                <HBox>
                                    <Button
                                        id="btnEditRule"
                                        text="编辑规则"
                                        icon="sap-icon://edit"
                                        press="onEditRule"
                                        class="sapUiTinyMarginEnd"/>
                                    <Button
                                        id="btnCancelEditRule"
                                        text="取消"
                                        icon="sap-icon://cancel"
                                        visible="false"
                                        press="onCancelEditRule"/>
                                </HBox>
                            </HBox>
                            <TextArea
                                id="rulePreview"
                                rows="8"
                                width="100%"
                                editable="false"/>
                        </VBox>
                    </HBox>
                </content>
            </Panel>

            <!--
            /*********************************
             * Results Section
             * 
             * Features:
             * - Expandable panel with search
             * - Status column with icons
             * - Row index for reference
             * - PO Number identification
             * - Detailed result message
             * 
             * Data Binding:
             * - Bound to results>/rows model
             * - Supports sorting and filtering
             *********************************/
            -->
            <Panel
                expandable="true"
                expanded="true"
                headerText="处理结果"
                width="auto"
                class="sapUiNoContentPadding">
                <headerToolbar>
                    <Toolbar style="Clear">
                        <Title text="处理结果"/>
                        <!-- 添加进度条和处理状态 -->
                        <ToolbarSpacer width="2rem"/>
                        <ProgressIndicator
                            id="processingProgress"
                            width="200px"
                            percentValue="{batch>/progress}"
                            displayValue="{batch>/progress}%"
                            showValue="true"
                            state="{= ${batch>/progress} === 100 ? 'Success' : 'Information'}"
                            class="sapUiTinyMarginEnd"/>
                        <Text text="{batch>/processedCount} of {batch>/totalCount} Processed" class="sapUiTinyMarginEnd"/>
                        <ToolbarSpacer/>
                        <SearchField 
                            id="resultsSearchField"
                            width="15rem" 
                            placeholder="Search results..." 
                            search="onResultsSearch"/>
                        <Button
                            icon="sap-icon://action-settings"
                            tooltip="Table Settings"
                            press="onTableSettingsPress"/>
                        <core:Icon src="sap-icon://table-view" class="sapUiTinyMarginEnd"/>
                    </Toolbar>
                </headerToolbar>
                <content>
                    <VBox class="sapUiSmallMargin">
                        <Table
                            id="resultsTable"
                            items="{
                                path: 'results>/rows',
                                sorter: {
                                    path: 'rowIndex',
                                    descending: false
                                }
                            }"
                            sticky="ColumnHeaders"
                            growing="true"
                            growingThreshold="1000"
                            growingScrollToLoad="true">
                            <columns>
                                <!-- 固定列 -->
                                <Column width="60px" hAlign="Begin"><Text text="行号" /></Column>
                                <Column width="60px" hAlign="Begin"><Text text="状态" /></Column>
                                <Column width="100px" hAlign="Begin"><Text text="结果" /></Column>
                                <!-- 根据客户显示动态列 -->
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001' || ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="客户代码" />
                                </Column>
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001' || ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="采购订单号" />
                                </Column>
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001' || ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="采购订单日期" />
                                </Column>
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001' || ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="物料编号" />
                                </Column>
                                <!-- 客户 A 特有列 -->
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001'}" hAlign="Begin">
                                    <Text text="采购订单行项目" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="客户物料编号" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="数量" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="需求日期" />
                                </Column>
                                <!-- 客户 A 特有列 -->
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C001'}" hAlign="Begin">
                                    <Text text="净价" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="物料编号" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="销售订单类型" />
                                </Column>
                                <!-- 客户 B 特有列 -->
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="工厂" />
                                </Column>
                                <Column visible="{= ${customerRules>/selectedCustomer} === 'C002'}" hAlign="Begin">
                                    <Text text="收货方" />
                                </Column>
                                <Column hAlign="Begin">
                                    <Text text="错误原因" />
                                </Column>
                            </columns>
                            <items>
                                <ColumnListItem>
                                    <cells>
                                        <Text text="{results>rowIndex}" />
                                        <!-- 修改：无论valid是true还是false都显示绿色对勾 -->
                                        <ObjectStatus
                                            text=""
                                            state="Success"
                                            icon="sap-icon://status-positive"/>
                                        <ObjectStatus
                                            text="{= ${results>result} === 'Processing Success' ? 'Processed' : 'Deleted' }"
                                            state="{= ${results>result} === 'Processing Success' ? 'Success' : 'Warning' }"
                                            icon="{= ${results>result} === 'Processing Success' ? 'sap-icon://accept' : 'sap-icon://decline' }"/>
                                        <Text text="{results>customerCode}" />
                                        <Text text="{results>poNumber}" />
                                        <Text text="{results>poDate}" />
                                        <Text text="{results>itemNo}" />
                                        <Text text="{results>poLine}" />
                                        <Text text="{results>customerPartNo}" />
                                        <Text text="{results>quantity}" />
                                        <Text text="{results>requestDate}" />
                                        <Text text="{results>netPrice}" />
                                        <Text text="{results>partNo}" />
                                        <Text text="{results>salesOrderType}" />
                                        <Text text="{results>plant}" />
                                        <Text text="{results>shipTo}" />
                                        <Text text="{= ${results>valid} ? '' : ${results>reason}}" />
                                    </cells>
                                </ColumnListItem>
                            </items>
                        </Table>
                    </VBox>
                </content>
            </Panel>
        </content>

        <footer>
            <Toolbar>
                <ToolbarSpacer/>
                <Button
                    text="下一步：销售订单上传"
                    type="Emphasized"
                    icon="sap-icon://navigation-right-arrow"
                    iconDensityAware="false"
                    press="onNavigateToSOUpload"
                    enabled="{= ${results>/rows/length} &gt; 0}"/>
            </Toolbar>
        </footer>
    </Page>
</mvc:View>
